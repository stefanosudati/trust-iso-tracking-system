# ── Trust ISO Tracking System ──────────────────────────────
# Copia questo file come .env e personalizza i valori:
#   cp .env.example .env
#
# Poi avvia il server:
#   npm run dev

# ── Obbligatorie ─────────────────────────────────────────

# Segreto JWT — CAMBIARE IN PRODUZIONE con una stringa lunga e casuale
# Genera con: openssl rand -base64 48
JWT_SECRET=CHANGE-ME-use-a-long-random-string

# ── Opzionali ─────────────────────────────────────────────

# Porta del server (default: 3002, impostato anche nel Dockerfile)
PORT=3002

# Percorso del database SQLite
# In sviluppo: ./data/db.sqlite (default)
# In Docker/Coolify: /data/db.sqlite (montato come volume)
DB_PATH=./data/db.sqlite

# ── Email (opzionali) ────────────────────────────────────
# Se configurate, l'app invia un riepilogo changelog a tutti gli admin.
# Servono almeno SMTP_HOST, SMTP_USER e SMTP_PASS per attivare le email.
#
# IMPORTANTE: NON usare la password dell'account email!
# Usare una "Password per le app" (App Password) di 16 caratteri.
#
# Come generare una Password per le app:
#
# Gmail:
#   1. Vai su https://myaccount.google.com/apppasswords
#      (richiede la verifica in 2 passaggi attiva)
#   2. Seleziona "Altra (nome personalizzato)" → scrivi "Trust ISO"
#   3. Clicca "Genera" → copia il codice di 16 caratteri (es. abcd efgh ijkl mnop)
#   4. Incollalo in SMTP_PASS senza spazi (es. abcdefghijklmnop)
#
# Outlook / Microsoft 365:
#   1. Vai su https://account.live.com/proofs/AppPassword
#   2. Genera una nuova password per app
#   3. Copia il codice di 16 caratteri in SMTP_PASS
#
# Altri provider: cerca "password per le app" o "app password"
#   nella documentazione del tuo provider email.

# Host del server SMTP
#SMTP_HOST=smtp.gmail.com

# Porta SMTP (default: 587 per STARTTLS, 465 per SSL)
#SMTP_PORT=587

# Indirizzo email mittente (es. nome@gmail.com)
#SMTP_USER=

# Password per le app (16 caratteri, senza spazi)
# NON la password dell'account! Vedi istruzioni sopra.
#SMTP_PASS=

# Indirizzo mittente visualizzato (default: uguale a SMTP_USER)
#SMTP_FROM=

# Frequenza email riepilogo changelog: "daily" o "weekly" (default: daily)
#CHANGELOG_EMAIL_INTERVAL=daily
